plugin: amazon.aws.aws_ec2

regions:
  - us-east-1

filters:
  tag:name: nginx
  instance-state-name: running

hostname: 
  - private-ip-address

compose:
# This sets the 'ansible_host' variable to connect with IP address without changing the hostname.
  ansible_host: private-ip-address

